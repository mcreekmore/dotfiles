-- Active colorscheme. To switch: change the source below and the required
-- module to another one in lua/themes/ (each documents its source URL).
vim.pack.add({ { src = "https://github.com/Mofiqul/vscode.nvim" } })

require("themes.vscode")
